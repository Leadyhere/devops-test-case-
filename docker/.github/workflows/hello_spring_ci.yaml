name: hello spring boot app to docker

on:
    #workflow_dispatch
    push

jobs:
    build-test:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            - name: Setup JDK
              uses: actions/setup-java@v5
              with:
                distribution: temurin
                java-version: '21'
            - name: Build and test
              working-directory: C:/Users/avira/Desktop/RESUME/codes/docker/Full-Stack-AI-DevOps-Masterclass/docker/app1-hello/springboot/hello-spring
              run: mvn -B clean verify
            - name: upload_test_reports
              uses: actions/upload-artifact@v7
              with:
                  name: test-reports
                  path: C:/Users/avira/Desktop/RESUME/codes/docker/Full-Stack-AI-DevOps-Masterclass/docker/app1-hello/springboot/hello-spring/target/surefire-reports
    docker-build-push:
        runs-on: ubuntu-latest
        needs: build-test

        steps:
            - name: Checkout code
              run: echo "hello from second workflowsssss"
            - name: Setup JDK
              run: echo "Environment $APP_ENV"
            - name: Build JAR for docker
              run: echo "file created successfully" 
            - name: login to docker
              run: echo ""
            - name: build and push docker image
              run: echo ""